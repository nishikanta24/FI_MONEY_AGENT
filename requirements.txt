# Existing MCP and core dependencies
requests
websocket-client
mcp

# Google ADK and Vertex AI dependencies
google-adk
google-cloud-aiplatform
vertexai
google-genai

# Additional data APIs for Phase 1
yfinance
alpha-vantage
pandas
numpy
# Note: asyncio is built into Python 3.7+, no need to install

# FastAPI dependencies - NEWLY ADDED
fastapi
uvicorn
python-multipart
pydantic

# CORS support (already included in FastAPI but explicit)
# fastapi already includes starlette

# Optional but recommended
python-dotenv  # For environment variables
httpx         # For HTTP client (if needed for testing)

# Development dependencies (optional)
pytest        # For testing
pytest-asyncio  # For async testing